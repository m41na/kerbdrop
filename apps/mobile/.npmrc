# Critical: prevent React Native packages from being hoisted to root
# Native modules MUST be physically present in this package's node_modules
# to be found by Metro bundler and native build tools
shamefully-hoist=false
hoist=false
public-hoist-pattern[]=*
