# Prevent hoisting issues globally
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Mobile app gets its own isolated node_modules
# (enforced per-package via apps/mobile/.npmrc)

# Block accidental npm/yarn usage
engine-strict=true
